python-telegram-bot[job-queue]==21.6
SpeechRecognition==3.10.4
pydub==0.25.1
gTTS==2.5.3
requests==2.32.3
python-dotenv==1.0.1

# Python 3.13 удалил встроенный модуль audioop, от которого зависит pydub.
# Этот пакет восстанавливает его (официальный бэкпорт). На более старых
# версиях Python строка ниже просто не устанавливается — это нормально.
audioop-lts; python_version >= "3.13"
